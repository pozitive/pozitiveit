- content_for :top do
  = render "layouts/topbarsmooth"
  = render "hero"
  = render "promo"
  = render 'portfolio'
  = render "hireus"

- content_for :bottom do
  = render "team"
  = render "tools"
  = render "price"
  = render "contact"



.row
  .columns.small-12
    = @work
  .columns.small-12
    = @process
