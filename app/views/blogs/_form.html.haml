.editblog
  = form_for @blog, :html => {:multipart => true} do |f|
    - if @blog.errors.any?
      #error_explanation
        %h2= "#{pluralize(@blog.errors.count, "error")} prohibited this blog from being saved:"
        %ul
          - @blog.errors.full_messages.each do |msg|
            %li= msg

    .field
      = f.label :name
      = f.text_field :name, {required: ''}
    .field
      = f.label :permalink
      = f.text_field :permalink, {required: ''}
    .field
      = f.file_field :image
    .field
      = f.label :remote_image_url, "or image URL"
      = f.text_field :remote_image_url
    .field
      = f.label :summary
      = f.text_field :summary, { placeholder: 'Maximum 255 symbols'}
    .field
      = f.label :content
      = f.text_area :content, {required: ''}
    .field
      = f.label :keyword
      = f.text_field :keyword, { placeholder: 'Meta keywords with commas separate'}
    .actions
      = f.submit 'Save', class: "button radius success"
